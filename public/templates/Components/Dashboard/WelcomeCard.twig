<div class="card">
	<div class="d-flex align-items-start row">
		<div class="col-sm-7">
			<div class="card-body">
				<h5 class="card-title text-primary mb-3">{{ 'Components.Dashboard.WelcomeCard.Card.Title'|translate(1, {
                    "{{ username }}": User.name
                    }) }}
					👋</h5>
				<p class="mb-6">{{ 'Components.Dashboard.WelcomeCard.Card.Text'|translate(1, {
                    "{{ tag }}": ENV.THEME_GIT_TAG,
                    "{{ commit }}": ENV.THEME_GIT_COMMIT,
                    }) }}</p>
			</div>
		</div>
		<div class="col-sm-5 text-center text-sm-left">
			<div class="card-body pb-0 px-0 px-md-6">
				<img src="{{ includeResource("img/illustrations/welcome.svg") }}" height="175" class="scaleX-n1-rtl">
			</div>
		</div>
	</div>
</div>
